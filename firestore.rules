rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allows public access for 30 days.
      allow read, write: if request.time < timestamp.date(2026, 10, 9);
    }
  }
}
